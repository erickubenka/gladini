1. Connect to to Selenium 4 Grid from your local machine
http://localhost:{{ .Values.hub.service.nodePort }}

2. Try to get the Ip of Selenium Grid 
kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}"

3. Connect to selex metrics via local machine
http://localhost:{{ .Values.selex.service.nodePort }}
